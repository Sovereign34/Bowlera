## Session: 3 — 2026-07-18
## Açıklama: `types/index.ts`'teki `CustomizerSelection` tipi eski 4 adımlı şemadan
(base/protein/toppings/sauce) CUSTOMIZER_SPEC.md v1.1'deki 5 adımlı şemaya
(base/main/mainPortion/garden/signatureFlavor/finish/extras) güncellendi. Ayrıca
customizer bileşen kataloğu için yeni tipler eklendi: `CustomizerComponentItem`,
`CustomizerExtraOptions`, `CustomizerCatalog`, `CustomizerTotals`. Sebep: Oturum 2'de
alınan mimari karara (4→5 adım geçişi, `20260718001123_customizer_5_adim_gecisi.md`)
göre kod tarafının kontratla senkronize edilmesi — Açık Sorun #10'un tip/kod kısmı bu
notla kapatılıyor; `ARCHITECTURE.md`/`DEPENDENCIES.md` dokümantasyon güncellemesi ayrı
görev olarak açık kalıyor.

### ÖNCE
```ts
export type CustomizerSelection = {
  base: string
  protein: string
  toppings: string[]
  sauce: string
}
```

### SONRA
```ts
export type CustomizerSelection = {
  base: string | null
  main: string | null
  mainPortion: "single" | "double"
  garden: string[]
  signatureFlavor: string | null
  finish: string[]
  extras: {
    extraAvocado: boolean
    extraSauce: boolean
    extraCrunch: boolean
  }
}

// + yeni tipler: CustomizerComponentItem, CustomizerExtraOptions,
//   CustomizerCatalog, CustomizerTotals (bkz. types/index.ts)
```

### ETKİLENEN DOSYALAR
- `types/index.ts` — bu notla birlikte tam dosya olarak güncellendi
- `lib/customizer-data.ts` — YENİ — boş katalog iskeleti, tüm ürün dizileri `TODO: PROD` işaretli
- `lib/customizer-pricing.ts` — YENİ — `CustomizerCatalog` parametresi alan saf hesaplama fonksiyonları
- `store/useCustomizerStore.ts` — YENİ — 5 adımlı state makinesi + guard
- `CartItem.customization` alanının tipi dolaylı olarak değişti (isim aynı, şekil farklı) — henüz
  hiçbir sepet bileşeni bu alanı okumuyor, bu session'da kırılma riski yok
- ⬜ `ARCHITECTURE.md` §2.4 ve `DEPENDENCIES.md` henüz güncellenmedi — Açık Sorun #10 kapsamında
  ayrı görev olarak kalıyor

### GERİ ALMA
`types/index.ts`'te `CustomizerSelection`'ı yukarıdaki "ÖNCE" bloğuna döndür;
`lib/customizer-data.ts`, `lib/customizer-pricing.ts`, `store/useCustomizerStore.ts`
dosyalarını sil. Henüz hiçbir UI bileşeni bu dosyalara bağlı değil (Oturum 4'te
bağlanacak) — geri alma sıfır yan etki taşır.
